# FastAPI / frontend
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Local runtime
QUANT_RUNTIME_DIR=backend/.runtime
QUANT_SQLITE_PATH=backend/.runtime/quant_platform.db

# Optional symbol mapping file
CODENAME_CSV_PATH=../codename.csv

# Optional pywencai cookie (can be auto-filled via `python scripts/update_wencai_cookie.py`)
WENCAI_COOKIE=your_browser_cookie_here
